name: sizer
description: Responsive UI solutions for Mobile, Web, and Desktop — making adaptability effortless.
version: 3.1.3

homepage: https://github.com/TechnoPrashant/Sizer
repository: https://github.com/TechnoPrashant/Sizer
issue_tracker: https://github.com/TechnoPrashant/Sizer/issues
topics:
  - flutter
  - responsive
  - ui
  - layout
  - adaptive

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# These files must exist in your repo at the given paths
screenshots:
  - description: With Sizer
    path: example/images/img_ss_with_lib.png
  - description: Without Sizer
    path: example/images/img_ss_without_lib.png

flutter:
# (No assets/fonts needed here unless you actually ship them)
